<form method="post" action="{{ path('app_adverts_admin_delete', {'id': advert.id}) }}">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ advert.id) }}">
    <button class="btn btn-danger btn-xl _deleteBtn mr-3"><i class="fa-solid fa-trash-can fa-lg text-white mr-3"></i>Supprimer cette annonce</button>
</form>
