<form method="post" action="{{ path('app_transactional_admin_delete', {'id': transactional.id}) }}">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ transactional.id) }}">
    <button class="btn btn-danger btn-xl _deleteBtn mr-3"><i class="fa-solid fa-trash-can fa-lg text-white mr-3"></i>Supprimer cet email transactionnel</button>
</form>
