{% extends 'admin/base_admin.html.twig' %}

{% block title %}Mail transactionnel{% endblock %}

{% block body %}
    <h1>Mail transactionnel</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Sujet</th>
                <td>{{ transactional.subject }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ transactional.description }}</td>
            </tr>
        </tbody>
    </table>
    <div class="ml-3">{{ template|raw }}</div>

    <a href="{{ path('app_transactional_admin_index') }}" class="btn btn-info mr-3"><i class="fa-solid fa-chevron-left fa-sm mr-3"></i>Retour</a>
    <div class="d-flex mt-3">
        <a href="{{ path('app_transactional_admin_edit', {'id': transactional.id}) }}" class="btn btn-info mr-3"><i class="fa-solid fa-pen-to-square fa-lg mr-3"></i>Editer</a>

        {{ include('admin/transactional_admin/_delete_form.html.twig') }}
    </div>
{% endblock %}
