{% extends 'base.html.twig' %}

{% block title %}PagesType{% endblock %}

{% block body %}
    <h1>PagesType</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ pages_type.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ pages_type.name }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ pages_type.type }}</td>
            </tr>
            <tr>
                <th>Has_version</th>
                <td>{{ pages_type.hasVersion ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_pages_type_admin_index') }}">back to list</a>

    <a href="{{ path('app_pages_type_admin_edit', {'id': pages_type.id}) }}">edit</a>

    {{ include('pages_type_admin/_delete_form.html.twig') }}
{% endblock %}
